//class and object

//class is just a blueprint of a program and doesnot occupy any memory where as when object is created for class it allocates the memory


/*
#include<iostream>
using namespace std;

class House 	//class variable declaration
{
	//access specifier
	private:		//we dont want to give of variable outside the class so it is specified as private(hide the data )
			
	//member variable
	int length,breadth;
	
	//member function
	public:		//the member is accessed by setdata only and it provides the data to them 
	
	void setdata(int x,int y)	//to give data for length and breadth
	{
		length=x;
		breadth=y;
	}
	
	void area()		//to print the house area
	{
		cout<<"\n Area is "<<length*breadth;
	}
};

int main()
{
	House h1;		//object for house i.e, h1 is created which will allocate the memory
	
	h1.setdata(20,10);
	h1.area();
	
	return 0;
}
*/
